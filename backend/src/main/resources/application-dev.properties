spring.datasource.url=${DEV_SUPABASE_URL_TRANSACTION_POOLER}
spring.datasource.username=${DEV_SUPABASE_USERNAME}
spring.datasource.password=${DEV_SUPABASE_PASSWORD}

# Emails
app.email.enabled=true
app.email.override-recipient=${EMAIL_OVERRIDE_RECIPIENT}